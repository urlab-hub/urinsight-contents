// Read-only reuse of the approved Daily operating values. Never import renderer code.
import { designTokens, coverOverlay } from '../../../src/config/tokens.js';
export const t = designTokens(true);
export { coverOverlay };
export const frameGap = 100;
export const sectionPadding = 80;
